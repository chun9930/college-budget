## 오늘 한 일
- mock auth의 회원가입/로그인 저장 구조를 name, email, password 기준으로 통일했다.
- 이메일 형식이 아니면 회원가입/로그인이 바로 막히도록 Signup.jsx와 Login.jsx에 공통 emailRegex 검증을 추가했다.
- 회원가입 성공 후 자동 로그인하지 않고 /login으로 이동하도록 유지했다.
- 로그인 성공 시 / 로 이동하고, 실패 시 인라인 에러 메시지를 보여주도록 정리했다.
- 마이페이지의 데이터 초기화가 서비스 데이터만 지우고 users/loginState/userProfile은 유지하도록 분리했다.
- 마이페이지에서 데이터 초기화 후에도 로그아웃되지 않고 현재 페이지에 머물도록 수정했다.
- Header와 BottomNav는 로그인 상태에서만 보이도록 유지했다.
- App, Login, Signup, MyPage, auth 관련 테스트를 새 흐름에 맞춰 정리했다.
- esbuild로 App, auth, Login, Signup, MyPage 및 관련 테스트의 문법을 확인했다.
- Vitest는 Windows 환경의 spawn EPERM 문제로 실행이 완료되지 않았다.

## 다음에 해야할 일
- 브라우저에서 회원가입/로그인/로그아웃/데이터 초기화 흐름을 직접 확인한다.
- README.md의 로그인 우선 구조와 데이터 초기화 분리 설명을 검토한다.
- 필요하면 MyPage 테스트에서 데이터 초기화 후 현재 페이지 유지 여부를 한 번 더 보강한다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 통계 화면의 소비 패턴 분석과 개인화 피드백을 강화했다.
- `src/lib/budget.js`에 카테고리별 지출 요약, 전월 대비 비교, 과소비 피드백 생성 helper를 추가했다.
- `Statistics.jsx`를 재구성해 이번 달 총지출, 일별 막대, 카테고리 비중, 전월 비교, 피드백 카드가 함께 보이도록 했다.
- `sourceRecurringId`가 있는 자동 정기지출은 통계에는 표시되지만 예산 계산과는 구분된다는 안내 문구를 유지했다.
- `src/lib/budget.analysis.test.js`와 `src/pages/Statistics.test.jsx`를 추가해 0원, 40% 이상 카테고리, 전월 증가, 자동 정기지출 포함/제외 흐름을 검증하도록 했다.
- `README.md`의 분석 리포트 설명을 현재 구현에 맞게 보강했다.
- esbuild로 문법 검증을 통과했고, Node 직접 실행으로 helper 결과를 확인했다.
- Vitest는 Windows 환경의 `spawn EPERM` 오류로 계속 실행이 막혔다.

## 다음에 해야할 일
- 브라우저에서 Statistics 화면의 새 피드백 카드와 전월 대비 문구를 수동으로 확인한다.
- 12번 대학생 할인/혜택 피드백 추가로 넘어간다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 목표 목록 구조 확장 작업의 마무리 검증을 진행했다.
- `App.jsx`에서 `savingGoals`가 `totalFixedExpense`, `dailyBudget`, `budgetMonthSpent` 계산에 직접 들어가지 않는다는 점을 다시 확인했다.
- `BudgetSettings.jsx`에서 목표 목록 카드가 `savingGoals` 배열과 `onSavingGoalsChange`로 연결되어 추가/수정/삭제 흐름을 타는 것을 코드 기준으로 확인했다.
- `savingGoalSettings`는 기존 단일 목표 계산용으로 유지되고, `savingGoals`는 별도 목록 관리용으로 분리된 것을 재확인했다.
- `calculateSavingGoalListSummary()`를 Node 직접 실행으로 다시 검증해 정상 목표, 0 목표, 초과 저축 목표의 안전 계산을 확인했다.
- Vitest는 Windows 환경에서 `spawn EPERM`이 동일하게 재현되어 테스트 실행이 끝까지 진행되지 않았다.
- 브라우저 직접 확인은 이번 마무리 검증에서는 수행하지 않았다.

## 다음에 해야할 일
- 11번 소비 패턴 분석과 개인화 피드백 강화 작업으로 넘어간다.
- Vitest가 계속 막히는 환경 제약은 이후 작업에서도 Node/esbuild 기반 보조 검증으로 보완한다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 목표 기능을 `savingGoals` 배열 기반의 목록 구조로 확장했다.
- `src/lib/budget.js`에 목표 목록 요약 계산 헬퍼를 추가해 목표별 남은 금액, 남은 일수, 월별 필요 저축액, 하루 필요 저축액, 달성률을 계산하도록 했다.
- `src/lib/storage.js`에 `savingGoals` 저장 키를 추가하고, 시드 데이터와 계정 스냅샷에 목표 목록이 함께 저장되도록 연결했다.
- `src/lib/seedData.js`에 두 테스트 계정의 `savingGoals` 목록을 추가했다.
- `BudgetSettings.jsx`에 목표 기반 재무 계획 카드 목록과 추가/수정/삭제 폼을 넣어 여러 목표를 관리할 수 있게 했다.
- `App.jsx`에서 `savingGoals` 상태를 계정 스냅샷과 저장 흐름에 포함시켰다.
- `App.test.jsx`, `BudgetSettings.test.jsx`, `seedData.test.js`에 목표 목록 계산과 저장/수정/삭제 흐름을 검증하는 테스트를 추가했다.
- `README.md`에 `savingGoals` 저장 키와 목표 목록 구조 설명을 반영했다.
- esbuild로 주요 파일 문법을 확인했고, 목표 계산 헬퍼는 Node 직접 실행으로 결과를 검증했다.
- Vitest는 현재 Windows 환경에서 `spawn EPERM` 오류로 실행이 완료되지 않았다.

## 다음에 해야할 일
- 브라우저에서 BudgetSettings의 목표 목록 카드가 실제로 보이는지 확인한다.
- 목표 추가/수정/삭제 후 `savingGoals`가 계정 스냅샷에 정상 반영되는지 점검한다.
- 다음 단계인 11번 소비 패턴 분석과 개인화 피드백 강화를 진행한다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 예산 설정에서 근무 조건 기반 예상 월수입 계산식을 `시급 × 하루 근무시간 × 주 근무일수 × 4.345`로 변경했다.
- 근무 조건 계산 안내 문구를 추가해서 직접 입력 모드와 계산 적용 모드의 차이를 분리했다.
- 근무 조건 계산 결과가 저장되면 `monthlyIncome`에 반영되도록 유지했다.
- 직접 입력 모드에서 사용자가 입력한 `monthlyIncome`이 그대로 저장되는 흐름을 테스트로 보강했다.
- BudgetSettings의 월수입 계산 관련 테스트 기대값을 4.345 기준으로 수정했다.
- README.md의 월수입 계산 설명을 현재 구현에 맞게 갱신했다.
- esbuild와 Vitest로 BudgetSettings/App 관련 문법과 테스트를 확인했다.

## 다음에 해야할 일
- 브라우저에서 직접 입력 모드와 근무 조건 계산 모드를 번갈아 전환해 월수입 저장값이 자연스럽게 보이는지 확인한다.
- Home의 오늘 예산과 BudgetSettings의 예산 미리보기가 같은 `monthlyIncome` 기준으로 유지되는지 다시 점검한다.
- 다음 단계인 목표 기능 확장 작업으로 넘어갈 준비를 한다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 8번 자동 이월 로직을 월 변경 기준으로 수정했다.
- `src/lib/budget.js`의 `calculateAutomaticCarryOver()`에서 기존 `carryOver`를 다시 더하지 않도록 정리했다.
- `src/App.jsx`에 계정별 `carryOverState`와 월별 자동 이월 스냅샷 재사용 구조를 추가했다.
- `src/pages/BudgetSettings.jsx`에 이번 달 자동 이월은 한 번만 계산되고 같은 달에는 저장된 값을 재사용한다는 안내 문구를 추가했다.
- `src/pages/BudgetSettings.test.jsx`, `src/App.test.jsx`에 자동 이월 계산 기준을 검증하는 테스트를 추가했다.
- README.md에 자동 이월 월별 스냅샷 설명을 반영했다.
- esbuild로 `budget.js`, `App.jsx`, `BudgetSettings.jsx` 문법 검증을 통과했다.
- Node 직접 실행으로 `calculateAutomaticCarryOver()`가 carryOver를 다시 더하지 않고 `30000`을 반환하는 것을 확인했다.
- Vitest는 이번 작업에서도 Windows 환경의 `spawn EPERM`으로 실행 실패했다.

## 다음에 해야할 일
- 브라우저에서 자동 이월 안내 문구와 이번 달 예산 표시가 자연스럽게 보이는지 확인한다.
- 9번 근무 조건 기반 예상 월수입 계산과 자동 이월 결과가 충돌하지 않는지 점검한다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 8번 자동 이월 로직을 월 변경 기준으로 수정했다.
- `src/lib/budget.js`의 자동 이월 계산식에서 기존 `carryOver`를 다시 더하지 않도록 정리했다.
- `src/App.jsx`에 계정별 `carryOverState`와 현재 월 스냅샷 재사용 구조를 추가했다.
- 같은 달에는 저장된 자동 이월 값을 재사용하고, 월이 바뀌면 다시 계산하도록 처리했다.
- `src/pages/BudgetSettings.jsx`에 이번 달 자동 이월이 월별로 한 번만 계산된다는 안내 문구를 추가했다.
- `src/pages/BudgetSettings.test.jsx`, `src/App.test.jsx`에 자동 이월 기준 검증을 추가했다.
- README.md에 자동 이월 월별 스냅샷 설명을 반영했다.
- esbuild로 alert, budget, App, BudgetSettings 관련 파일 문법을 확인했다.
- Vitest는 이번 작업에서도 Windows 환경의 `spawn EPERM`으로 실행 실패했다.

## 다음에 해야할 일
- 브라우저에서 자동 이월 안내 문구와 오늘/이번 달 예산 값이 자연스럽게 보이는지 확인한다.
- 9번 근무 조건 기반 예상 월수입 계산을 다시 점검한다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 7번 하루/월 예산 초과 알림 분리를 구현했다.
- `src/lib/alert.js`에 월 기준 판단용 `getMonthlyJudgmentSnapshot()`을 추가했다.
- `src/App.jsx`에서 월 예산 기준(`monthlyBudgetBase`)과 월 판단 상태를 별도로 계산해 `BudgetSettings`에 전달했다.
- `src/pages/BudgetSettings.jsx`에 이번 달 예산 경고 배너를 추가해 홈의 오늘 예산 판단과 분리해 보여주도록 했다.
- `src/pages/ExpenseRecords.jsx`의 저장 전 판단은 기존처럼 오늘 기준으로 유지했다.
- `src/pages/ExpenseRecords.test.jsx`, `src/pages/BudgetSettings.test.jsx`, `src/App.test.jsx`에 새 판단 기준 검증을 추가했다.
- README.md에 예산 설정 화면의 이번 달 예산 경고 표시를 반영했다.
- esbuild로 alert, App, BudgetSettings 관련 파일의 문법을 확인했다.
- Vitest로 `App.test.jsx`와 `BudgetSettings.test.jsx`를 실행해 23개 테스트 통과를 확인했다.

## 다음에 해야할 일
- 브라우저에서 홈의 오늘 예산 판단과 예산 설정의 이번 달 예산 경고가 서로 다른 기준으로 보이는지 확인한다.
- 8번 자동 이월 로직 수정으로 넘어간다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 6번 실시간 소비 가능 여부 판단 개선을 시작했다.
- `src/lib/alert.js`에 지출 입력용 `getExpensePreviewSnapshot()`을 추가해 홈 판단과 입력 판단의 기준을 분리했다.
- `ExpenseRecords.jsx`의 금액 입력 아래 판단 문구를 공통 helper 기준으로 바꿔 저장 전 판단을 보여주도록 했다.
- `App.jsx`에서 `hasBudgetSetup`을 지출 기록 페이지에도 전달해 예산 설정 없음 상태를 더 정확히 판단하도록 했다.
- `ExpenseRecords.test.jsx`에 지출 입력 판단 helper를 검증하는 테스트를 추가했다.
- `BudgetSettings.jsx`와 `Statistics.jsx`의 기존 기준은 유지했다.
- esbuild로 수정 파일들의 번들 문법 검증을 통과했다.
- Vitest는 현재 Windows 환경에서 `spawn EPERM`이 반복되어 실행 검증이 막혔다.

## 다음에 해야할 일
- 브라우저에서 지출 기록 금액 입력 아래 판단 문구가 실제로 자연스럽게 보이는지 확인한다.
- 6번 작업의 추가 UI 문구가 더 필요한지 점검한 뒤, 다음 단계로 넘어간다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 5번 의미 분리 작업에서 남은 UI 혼동 리스크를 최소 수정으로 정리했다.
- `ExpenseRecords.jsx`의 일반 지출 목록에서 `sourceRecurringId`가 있는 항목만 `자동 정기지출` 배지와 `예산 계산 중복 제외` 보조 문구를 표시하도록 했다.
- `ExpenseRecords.jsx`의 정기지출 영역 설명을 다듬어 수동 고정지출과 별도 저장된다는 점이 더 분명하게 보이도록 했다.
- `BudgetSettings.jsx`의 수동 고정지출 설명과 최종 고정지출 설명을 정리해 수동 입력값과 자동 반영 정기지출의 관계가 드러나도록 했다.
- `Statistics.jsx`의 안내 문구를 기록 포함 / 예산 계산 중복 제외 의미로 더 자연스럽게 바꿨다.
- `index.css`에 자동 정기지출 배지용 최소 스타일을 추가했다.
- README.md의 예산 설정/통계 설명을 현재 표시 기준에 맞게 정리했다.
- Vitest로 기존 예산 설정/앱 테스트를 다시 실행해 통과를 확인했다.

## 다음에 해야할 일
- 브라우저에서 일반 지출 목록의 자동 정기지출 배지와 예산 계산 중복 제외 문구가 자연스럽게 보이는지 확인한다.
- 6번 실시간 소비 가능 여부 판단 개선으로 넘어간다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 일반 지출, 수동 고정지출, 정기지출의 의미를 화면 문구로 분리했다.
- `ExpenseRecords.jsx`에 일반 지출은 실제 생활 소비 기록, 정기지출은 매달 반복되는 고정비라는 설명을 보강했다.
- 자동 반영된 정기지출 기록은 기록 화면에서 남아도 예산 계산에서는 중복 제외된다는 안내를 추가했다.
- `BudgetSettings.jsx`에 수동 고정지출이 예산 계산 전에 미리 빠지는 계획 지출이라는 설명을 추가하고, 정기지출 합계와 분리해 보이도록 정리했다.
- `Statistics.jsx`는 자동 반영된 정기지출이 예산 계산에서 중복 제외된다는 안내 문구를 유지했다.
- `index.css`에 설명 문구와 보조 안내 문구를 위한 최소 스타일을 추가했다.
- `README.md`에 수동 고정지출과 정기지출의 역할 차이를 한 줄 보강했다.
- `npm.cmd exec -- vitest run src/App.test.jsx src/pages/BudgetSettings.test.jsx --environment jsdom --pool=threads --poolOptions.threads.singleThread` 실행으로 22개 테스트 통과를 확인했다.

## 다음에 해야할 일
- 브라우저에서 일반 지출/정기지출 영역 제목과 보조 문구가 실제로 잘 읽히는지 확인한다.
- Home과 BudgetSettings의 오늘 예산 미리보기와 최종 고정지출 문구가 같은 계산 기준으로 보이는지 수동 확인한다.
- 다음 단계에서는 6번 실시간 소비 가능 여부 판단 개선을 진행한다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 자동 반영 ON/OFF 상태에서 계산 기준과 화면 표시 기준이 다르게 보일 수 있는 리스크를 정리했다.
- `Statistics.jsx`에 `자동 반영된 정기지출은 예산 계산에서 중복 제외됩니다.` 안내 문구를 추가해 `sourceRecurringId` 기록의 예산 계산 제외 기준을 사용자에게 보여주도록 했다.
- `App.jsx`가 이미 `totalFixedExpense`, `manualFixedExpenseAmount`, `recurringTotal`, `budgetMonthSpent`, `displayMonthSpent`를 동일한 기준으로 계산하고 있음을 다시 확인했다.
- `README.md`에 통계 화면 안내 문구 기준을 한 줄 보강했다.
- `App.test.jsx`와 `BudgetSettings.test.jsx`를 다시 실행해 변경 후에도 22개 테스트가 통과하는 것을 확인했다.

## 다음에 해야할 일
- 브라우저에서 통계 화면의 안내 문구가 실제 현재 달 기록이 있을 때만 보이는지 확인한다.
- Home과 BudgetSettings의 오늘 예산 미리보기와 최종 고정지출이 같은 기준으로 보이는지 브라우저에서 비교 확인한다.
- 다음 단계에서는 5번 `일반지출/수동 고정지출/정기지출 의미 분리`를 이어서 진행한다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 예산 설정의 정기지출 자동 반영 기준을 `수동 고정지출 + recurringTotal` 구조로 정리했다.
- `App.jsx`에서 예산 계산용 지출 합계와 화면 표시용 지출 합계를 분리했다.
- 정기지출 자동 반영이 켜진 경우 `sourceRecurringId`가 있는 지출 기록은 예산 계산용 월 지출 합계에서 제외하도록 정리했다.
- `BudgetSettings.jsx`에서 정기지출 합계 불러오기 버튼을 제거하고, 수동 고정지출/정기지출 합계/최종 고정지출 안내만 남겼다.
- `BudgetSettings.test.jsx`의 중복 텍스트 매처를 정리해 버튼 제거와 저장 검증이 안정적으로 동작하도록 수정했다.
- `App.test.jsx`에 자동 반영 ON/OFF에서 `sourceRecurringId` 기록이 예산 계산에 중복 반영되지 않는지 확인하는 테스트를 추가했다.
- `README.md`의 예산 계산 기준과 정기지출 반영 설명을 현재 구현에 맞게 일부 갱신했다.
- `npm.cmd exec -- vitest run src/pages/BudgetSettings.test.jsx src/App.test.jsx --environment jsdom --pool=threads --poolOptions.threads.singleThread` 실행으로 22개 테스트 통과를 확인했다.

## 다음에 해야할 일
- 브라우저에서 자동 반영 ON/OFF일 때 오늘 예산 미리보기와 홈 표시값이 동일한지 수동 확인한다.
- 정기지출 자동 반영을 켠 상태에서 sourceRecurringId가 붙은 기록이 통계/기록 화면에서는 정상 표시되고 예산 계산에서는 중복 차감되지 않는지 점검한다.
- 아직 보류한 자동 이월, 분석 피드백, 목표 목록 확장은 다음 단계로 분리한다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 예산 설정 페이지에 월 수입 입력 방식을 `직접 입력`과 `근무 조건 계산`으로 나누는 UI를 추가했다.
- 근무 조건 계산 모드에서 시급, 하루 근무시간, 주 근무일수를 입력하면 `시급 × 하루 근무시간 × 주 근무일수 × 4` 기준으로 예상 월 수입을 계산해 저장하도록 연결했다.
- 예산 설정 페이지에 `정기지출 자동 반영` 토글을 추가했다.
- 자동 반영이 켜져 있으면 정기지출 합계 불러오기 버튼이 비활성화되고, 안내 문구가 자동 반영 상태에 맞게 바뀌도록 수정했다.
- `App.jsx`에서 정기지출 자동 반영이 켜진 경우 저장된 고정지출 값과 `recurringExpenses` 합계를 함께 사용해 하루 예산 계산에 반영하도록 분기했다.
- 기존 계정 스냅샷이나 localStorage에 새 필드가 없어도 깨지지 않도록 `DEFAULT_BUDGET_SETTINGS` 병합 로직을 보강했다.
- `src/pages/BudgetSettings.test.jsx`와 `src/App.test.jsx`에 근무 조건 계산 저장, 정기지출 자동 반영 ON/OFF, 합계 불러오기 비활성화 테스트를 추가하고 통과시켰다.
- `README.md`에 월 수입 입력 방식, 정기지출 자동 반영, 관련 storage 키를 반영했다.
- `npm.cmd exec -- vitest run src/pages/BudgetSettings.test.jsx src/App.test.jsx --environment jsdom --pool threads` 실행으로 24개 테스트 통과를 확인했다.

## 다음에 해야할 일
- 브라우저에서 예산 설정 화면의 직접 입력/근무 조건 계산 전환 흐름과 저장 후 홈 반영 결과를 수동으로 확인한다.
- 정기지출 자동 반영을 켠 상태에서 정기지출 수정 후 하루 예산이 기대한 값으로 바뀌는지 브라우저에서 점검한다.
- 보류한 추가 기능 계획에서 자동 이월과 분석 피드백 고도화 작업 순서를 다시 정리한다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 예산 계산 예외 처리 작업을 이어서 마무리했다.
- `App.jsx`에서 정기지출 합계를 고정지출에 자동 합산하지 않도록 분리하고, 저장된 고정지출 값만 하루 예산 계산에 반영하도록 정리했다.
- `budget.js`에 자동 이월 계산 헬퍼를 추가하고, `App.jsx`에서 이전 달 지출 기준 자동 이월 금액과 수동 이월 금액을 구분해 전달하도록 연결했다.
- `BudgetSettings.jsx`에서 빈 금액 필드를 저장 시 0으로 정규화하고, 목표 설정과 수동 이월 토글이 유효한 값이 없으면 저장 시 자동으로 OFF 처리되도록 보완했다.
- 예산 설정 화면에 자동 계산 이월 금액 패널과 수동 이월 입력 토글 UI를 추가했다.
- 홈 화면의 고정지출 안내 문구를 실제 동작과 맞게 `저장된 고정지출 기준`으로 수정했다.
- `README.md`에 수동 이월 관련 storage 키와 예산 설정/이월 동작 변경 사항을 반영했다.
- `BudgetSettings.test.jsx`, `Home.test.jsx`, `App.test.jsx`를 현재 UI와 저장 정책에 맞게 보정하고 jsdom 환경에서 테스트가 통과하도록 정리했다.
- `npm.cmd exec -- vitest run src/pages/BudgetSettings.test.jsx --environment jsdom --pool threads` 실행으로 11개 테스트 통과를 확인했다.
- `npm.cmd exec -- vitest run src/pages/Home.test.jsx src/App.test.jsx --environment jsdom --pool threads` 실행으로 13개 테스트 통과를 확인했다.
- `npm.cmd exec esbuild -- src/App.jsx src/pages/BudgetSettings.jsx src/pages/Home.jsx src/lib/budget.js --loader:.jsx=jsx --outdir=tmp-check --log-level=error`로 주요 파일 문법을 확인했고, 생성된 `tmp-check`는 정리했다.

## 다음에 해야할 일
- 브라우저에서 예산 저장 전/후 홈과 예산 설정의 오늘 예산 미리보기가 같은 기준으로 보이는지 수동 점검한다.
- 자동 이월 계산이 실제 요구사항과 맞는지 확인하고, 필요하면 다음 작업에서 일별 자동 이월과 월간 자동 이월을 더 구체화한다.
- 보류한 계획 문서 `추가 기능 중 부족한 부분 구현`을 다시 꺼내 추가 기능 작업 순서를 정한다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 이월 기능 확인 방법을 코드 기준으로 점검했다.
- `budget.js`, `App.jsx`, `BudgetSettings.jsx`에서 이월 금액이 하루 예산 계산에 들어가는 흐름을 확인했다.
- 현재 구현은 전날 남은 금액을 자동 계산하는 방식이 아니라, 예산 설정의 `carryOverEnabled`와 `carryOverAmount`를 저장 후 계산식에 반영하는 방식임을 정리했다.
- 수동 하루 예산 사용이 켜져 있으면 이월 금액보다 수동 예산이 우선 적용되는 점을 확인했다.

## 다음에 해야할 일
- 실제 자동 이월 기능이 필요하면 전날 예산과 전날 지출 차액을 계산해 `carryOverAmount`에 반영하는 별도 요구사항으로 분리한다.
- 브라우저에서 이월 금액 0원 저장과 100,000원 저장을 비교해 홈의 오늘 사용 가능 금액 변화 여부를 확인한다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 로그인 화면의 테스트 계정 안내와 실제 users 데이터가 일치하지 않을 수 있는 문제를 수정했다.
- seedMockDataIfNeeded()가 기존 users를 덮어쓰지 않고 email 기준으로 테스트 계정만 병합하도록 바꿨다.
- App 초기화 시 seed 함수를 마운트 기준으로 1회 호출하도록 옮겨 테스트 환경에서도 다시 seed가 들어가게 했다.
- seedData.test.js에 기존 users 보존, 중복 방지, 실제 로그인 가능 여부를 확인하는 테스트를 보강했다.
- README.md에 seed 병합 방식과 mock auth 테스트 계정 안내를 추가했다.
- esbuild로 seed/storage/App/Login/Signup/test 파일의 문법을 확인했다.
- Vitest는 Windows 환경의 spawn EPERM 문제로 실행이 완료되지 않았다.

## 다음에 해야할 일
- 브라우저에서 1234@naver.com / 1234, student@pingo.com / 12345 로그인 가능 여부를 다시 확인한다.
- users가 이미 있는 상태에서도 테스트 계정만 추가되는지 확인한다.
- 필요하면 seedData.test.js에서 계정별 스냅샷 검증을 더 늘린다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- 로그인 화면의 테스트 계정 안내와 실제 localStorage users 데이터가 어긋날 수 있는 문제를 확인했다.
- seedMockDataIfNeeded()를 전체 덮어쓰기가 아니라 users와 mockAccountData를 email 기준으로 병합하는 방식으로 바꿨다.
- 기존 users는 그대로 두고, 테스트 계정 2개만 없을 때 추가되도록 수정했다.
- seed 단계에서는 loginState와 userProfile이 생성되지 않도록 유지했다.
- seedData.test.js에 중복 생성 방지와 기존 users 보존 테스트를 추가했다.
- README.md에 seed 계정 병합 방식과 mock auth 테스트 계정 안내를 반영했다.
- esbuild로 seed 관련 파일과 로그인/회원가입 파일의 문법을 확인했다.
- Vitest는 Windows 환경의 spawn EPERM 문제로 실행이 완료되지 않았다.

## 다음에 해야할 일
- 브라우저에서 1234@naver.com / 1234, student@pingo.com / 12345 로그인 가능 여부를 확인한다.
- 기존 users가 있어도 테스트 계정만 추가되는지 확인한다.
- 필요하면 seed 계정 안내 문구와 README 설명을 한 번 더 정리한다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- mock auth 테스트 계정과 기능 확인용 seed 데이터를 추가했다.
- `src/lib/seedData.js`에 테스트 계정 2개와 계정별 예산, 정기지출, 지출 기록 원본을 분리해 저장했다.
- `src/lib/storage.js`에 `seedMockDataIfNeeded()`와 계정별 스냅샷 저장/조회 함수를 추가했다.
- 앱 시작 시 seed 함수가 한 번만 호출되도록 `App.jsx`에서 초기화 흐름을 연결했다.
- 로그인 화면에 테스트 계정 안내 박스를 추가했다.
- seed 데이터 전용 테스트 파일을 추가했고, 기존 `auth.test.js`는 삭제하지 않았다.
- README.md에 mock auth 테스트 계정과 seed 데이터 설명을 반영했다.
- esbuild로 seed 파일, storage, App, 로그인/회원가입 관련 파일의 문법을 확인했다.
- Vitest는 Windows 환경의 spawn EPERM 문제로 실행이 완료되지 않았다.

## 다음에 해야할 일
- 브라우저에서 테스트 계정 2개로 실제 로그인되는지 확인한다.
- 예산 설정의 정기지출 합계 불러오기와 seed 데이터 연동이 자연스럽게 보이는지 점검한다.
- 필요하면 seed 데이터의 설명 문구를 README에 더 보강한다.

## 깃허브 푸시기록
- 없음

## 오늘 한 일
- seedMockDataIfNeeded()와 login()을 Node에서 직접 검증했다.
- esbuild로 storage/auth 관련 파일을 번들링한 뒤 Node에서 mock localStorage를 붙여 실행했다.
- seed가 비어 있는 storage에서는 2개의 테스트 계정(1234@naver.com, student@pingo.com)을 users에 추가하는 것을 확인했다.
- 기존 users가 있어도 삭제하지 않고 테스트 계정만 email 기준으로 병합하는 것을 확인했다.
- seed 단계에서 loginState와 userProfile이 생성되지 않는 것을 확인했다.
- student@pingo.com / 12345와 1234@naver.com / 1234가 실제 로그인 성공하는 것을 확인했다.
- Vitest의 spawn EPERM 문제 대신 Node 직접 검증으로 핵심 흐름을 확인했다.

## 다음에 해야할 일
- 브라우저에서 로그인 화면의 테스트 계정 안내와 실제 로그인 가능 여부를 한 번 더 확인한다.
- 필요하면 seed 주입 시점(App 초기화)과 로그인 후 계정 스냅샷 복원 흐름을 브라우저에서 점검한다.

## 깃허브 푸시기록
- 없음
