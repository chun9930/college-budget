# 오늘 한 일
- 대학생 소비 판단 UI의 최신 기준에 맞춰 실제 React 구현을 진행했다.
- `localStorage` 기반 mock auth 로직을 추가하고 `users`, `loginState`, `userProfile` 저장 흐름을 만들었다.
- `/#/login`, `/#/signup`, `/#/my-page` 라우트를 추가하고 로그인 / 회원가입 / 마이페이지 화면을 구현했다.
- 예산 설정 화면을 3개 카드 구조로 다시 구성하고 목표 저축 / 기간별 계산 / 이월 토글을 반영했다.
- 홈 화면은 오늘 사용 가능 금액, 오늘 지출 금액, 경고 배너 중심으로 정리했다.
- 지출 기록 화면은 MVP 입력과 고급 옵션, 정기지출 등록, 템플릿 저장 / 불러오기를 분리했다.
- 분석 화면은 월 지출 요약, 카테고리 비중, 절약 힌트 중심으로 단순화했다.
- `README.md`도 실제 구현 구조에 맞게 `auth.js`와 새 라우팅을 반영해 정리했다.
- Vite 모듈 응답으로 `App.jsx`, 로그인 / 회원가입 / 예산 / 기록 / 분석 / 저장소 모듈이 정상 변환되는 것을 확인했다.
- `npm.cmd run test`는 현재 Windows + Vite 환경의 `spawn EPERM` 제한으로 완료하지 못했다.

# 다음에 해야할 일
- 브라우저에서 `http://localhost:5173/` 화면이 실제로 렌더되는지 확인한다.
- 필요하면 로그인 / 회원가입 / 마이페이지 문구와 데이터 초기화를 더 다듬는다.
- 테스트 환경의 `spawn EPERM` 문제를 우회할 방법이 있는지 추가로 확인한다.
- 작업이 마무리되면 커밋 메시지 승인을 먼저 받은 뒤 커밋한다.

# 깃허브 푸시기록
- `74a6a11` `First commit` - React + Vite 초기 구현과 기본 UI, 계산 로직, 문서 기초를 반영했다.
- `07bfc4e` `docs: revise mock auth spec and plan` - mock auth 정책, 새 라우팅, 예산 3카드 구조를 반영해 `PROJECT.md`와 구현 계획을 다시 정리했다.
- 이번 React 구현은 아직 커밋 전이며, 사용자 승인 후 `feat:` 계열 Conventional Commit으로 정리할 예정이다.

# 오늘 한 일
- 사용자의 승인 메시지를 받은 뒤 현재 변경사항을 `feat: mock auth 로그인/회원가입 및 마이페이지 추가`로 커밋했다.
- 커밋 `3af47be`를 `origin/main`에 푸시했다.
- Git의 dubious ownership 경고로 인해 `C:/college-budget`를 safe.directory로 등록한 뒤 푸시를 다시 진행했다.

# 다음에 해야할 일
- 브라우저에서 로그인, 회원가입, 마이페이지, 예산 설정, 지출 기록 화면이 정상 동작하는지 확인한다.
- 필요하면 입력 검증과 안내 문구를 다듬는다.
- 다음 기능 작업 전에는 항상 커밋 메시지를 먼저 사용자에게 승인받는다.

# 깃허브 푸시기록
- `3af47be` `feat: mock auth 로그인/회원가입 및 마이페이지 추가` - mock auth 로그인·회원가입, 마이페이지, 예산 설정 3카드 구조, 경고 배너, 지출 기록, 분석 화면 구현을 반영했다.

# 오늘 한 일
- 예산 설정 화면의 기능 토글 카드를 첨부 이미지처럼 보이도록 `ToggleRow` 컴포넌트로 분리했다.
- `BudgetSettings.jsx`에서 기능 토글을 체크박스 나열 대신 라벨 왼쪽, 스위치 오른쪽 구조로 바꿨다.
- `src/index.css`에 토글 스위치 전용 스타일과 모바일 대응 스타일을 추가했다.
- Vite 변환 확인으로 `BudgetSettings.jsx`, `ToggleRow.jsx`, `index.css`가 정상 응답하는 것을 확인했다.
- 토글 상태가 페이지 이동 후에도 유지되도록 즉시 저장 방식으로 바꿨다.
- `BudgetSettings.jsx`에서 토글 변경 시 부모 상태와 localStorage가 바로 갱신되도록 수정했다.
- 브랜드 로고 영역을 더 크게 보여 주도록 `brand-mark` 크기를 키웠다.
- 모바일에서는 로고가 너무 커지지 않도록 반응형 크기를 따로 두었다.

# 다음에 해야할 일
- 브라우저에서 `/#/budget-settings`를 새로고침해 토글 상태가 홈 이동 후에도 유지되는지 확인한다.
- 필요하면 스위치 크기나 카드 여백을 더 미세 조정한다.
- 다음 작업 전에는 커밋 메시지 승인을 먼저 받는다.

# 깃허브 푸시기록
- 아직 이번 토글 UI 수정은 커밋하지 않았다.

# 오늘 한 일
- 현재 워크트리의 변경 상태를 확인했다.
- 로고명과 로고 디자인 수정 요청을 반영해 현재 변경분을 원격 저장소에 푸시하기 위한 준비를 진행했다.
- 이번 작업은 코드 추가보다 푸시 및 기록 정리에 초점을 두었다.

# 다음에 해야할 일
- Toss + Banksalad 하이브리드 디자인 기준으로 화면별 구체 변경안을 정리한다.
- 캘린더 페이지 추가 범위와 데이터 표시 규칙을 확정한다.
- 이후 구현 변경이 생기면 README와 문서를 함께 동기화한다.

# 깃허브 푸시기록
- 아직 이번 작업에 대한 새 커밋/푸시는 완료되지 않았다.
