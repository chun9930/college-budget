# 오늘 한 일
- 대학생 소비 판단 UI의 최신 기준에 맞춰 실제 React 구현을 진행했다.
- `localStorage` 기반 mock auth 로직을 추가하고 `users`, `loginState`, `userProfile` 저장 흐름을 만들었다.
- `/#/login`, `/#/signup`, `/#/my-page` 라우트를 추가하고 로그인 / 회원가입 / 마이페이지 화면을 구현했다.
- 예산 설정 화면을 3개 카드 구조로 다시 구성하고 목표 저축 / 기간별 계산 / 이월 토글을 반영했다.
- 홈 화면은 오늘 사용 가능 금액, 오늘 지출 금액, 경고 배너 중심으로 정리했다.
- 지출 기록 화면은 MVP 입력과 고급 옵션, 정기지출 등록, 템플릿 저장 / 불러오기를 분리했다.
- 분석 화면은 월 지출 요약, 카테고리 비중, 절약 힌트 중심으로 단순화했다.
- `README.md`도 실제 구현 구조에 맞게 `auth.js`와 새 라우팅을 반영해 정리했다.
- Vite 모듈 응답으로 `App.jsx`, 로그인 / 회원가입 / 예산 / 기록 / 분석 / 저장소 모듈이 정상 변환되는 것을 확인했다.
- `npm.cmd run test`는 현재 Windows + Vite 환경의 `spawn EPERM` 제한으로 완료하지 못했다.

# 다음에 해야할 일
- 브라우저에서 `http://localhost:5173/` 화면이 실제로 렌더되는지 확인한다.
- 필요하면 로그인 / 회원가입 / 마이페이지 문구와 데이터 초기화를 더 다듬는다.
- 테스트 환경의 `spawn EPERM` 문제를 우회할 방법이 있는지 추가로 확인한다.
- 작업이 마무리되면 커밋 메시지 승인을 먼저 받은 뒤 커밋한다.

# 깃허브 푸시기록
- `74a6a11` `First commit` - React + Vite 초기 구현과 기본 UI, 계산 로직, 문서 기초를 반영했다.
- `07bfc4e` `docs: revise mock auth spec and plan` - mock auth 정책, 새 라우팅, 예산 3카드 구조를 반영해 `PROJECT.md`와 구현 계획을 다시 정리했다.
- 이번 React 구현은 아직 커밋 전이며, 사용자 승인 후 `feat:` 계열 Conventional Commit으로 정리할 예정이다.

# 오늘 한 일
- 사용자의 승인 메시지를 받은 뒤 현재 변경사항을 `feat: mock auth 로그인/회원가입 및 마이페이지 추가`로 커밋했다.
- 커밋 `3af47be`를 `origin/main`에 푸시했다.
- Git의 dubious ownership 경고로 인해 `C:/college-budget`를 safe.directory로 등록한 뒤 푸시를 다시 진행했다.

# 다음에 해야할 일
- 브라우저에서 로그인, 회원가입, 마이페이지, 예산 설정, 지출 기록 화면이 정상 동작하는지 확인한다.
- 필요하면 입력 검증과 안내 문구를 다듬는다.
- 다음 기능 작업 전에는 항상 커밋 메시지를 먼저 사용자에게 승인받는다.

# 깃허브 푸시기록
- `3af47be` `feat: mock auth 로그인/회원가입 및 마이페이지 추가` - mock auth 로그인·회원가입, 마이페이지, 예산 설정 3카드 구조, 경고 배너, 지출 기록, 분석 화면 구현을 반영했다.

# 오늘 한 일
- 지출 기록 화면에서 수동 템플릿 저장/불러오기를 제거하고 `최근 기록으로 빠른 입력` 방식으로 바꿨다.
- 최근 지출 기록을 10개까지 보여 주도록 조정하고, 기록을 누르면 금액/카테고리/결제수단이 폼에 채워지도록 구현했다.
- 카테고리 선택 시 같은 카테고리의 가장 최근 결제수단이 기본값으로 들어가도록 정리했다.
- `expenseTemplates` 저장 키와 초기화 코드의 남은 참조를 제거했다.
- `README.md`와 `docs/PROJECT.md`에서 템플릿 문구를 최근 기록 기반 자동 입력 보조로 수정하고, 최근 기록 기준을 10개로 맞췄다.

# 다음에 해야할 일
- 브라우저에서 최근 기록 10개가 보이고 클릭 시 폼이 채워지는지 확인한다.
- 필요하면 최근 기록 영역의 밀도와 여백을 미세 조정한다.
- 다음 작업 전에는 커밋 메시지 승인을 먼저 받는다.

# 깃허브 푸시기록
- 이번 최근 기록 10개 반영 작업은 아직 커밋/푸시하지 않았다.

# 오늘 한 일
- 정기지출 목록에 수정/삭제 버튼을 추가하고, 수정 시 `정기지출 수정 중` 배너와 `정기지출 수정 저장` 버튼이 보이도록 반영했다.
- `editingRecurringId` 상태를 기준으로 정기지출 수정 저장은 id 기반 업데이트만 수행하도록 분리했다.
- 정기지출 삭제 시 localStorage에 반영되도록 `App.jsx`에 삭제/수정 핸들러를 추가했다.
- 수정 취소 버튼을 추가해 수정 모드를 해제하고 입력값을 초기화하도록 정리했다.
- 최근 기록 자동 입력과 일반 지출 저장 흐름은 건드리지 않았다.
- `README.md`와 `docs/PROJECT.md`의 지출 기록 섹션에 정기지출 수정/삭제와 최근 기록 10개 기준을 반영했다.
- 정기지출 버튼 표시와 수정 모드 문구가 렌더 코드에 정상 포함된 것을 확인했다.
- `vitest`와 `build`는 현재 Windows 환경의 `spawn EPERM` 제한으로 끝까지 완료하지 못했다.

# 다음에 해야할 일
- 브라우저에서 정기지출 수정/삭제 버튼과 수정 모드 배너가 정상 동작하는지 확인한다.
- 필요하면 정기지출 목록의 버튼 간격과 배너 문구를 더 다듬는다.
- 다음 작업 전에는 커밋 메시지 승인을 먼저 받는다.

# 깃허브 푸시기록
- 이번 정기지출 수정/삭제 보강 작업은 아직 커밋/푸시하지 않았다.

# 오늘 한 일
- 일반 지출 기록 목록에 수정/삭제 버튼을 추가하고, `editingExpenseId` 기준으로 일반 지출 수정 모드를 분리했다.
- 일반 지출 수정 시 `지출 기록 수정 중` 배너와 `지출 수정 저장`, `수정 취소` 버튼이 보이도록 반영했다.
- 일반 지출 수정 저장은 새 항목 추가가 아니라 기존 `expenseRecord.id`를 유지한 채 `map` 업데이트로 동작하게 연결했다.
- 일반 지출 삭제는 `expenseRecords`에서 해당 id를 제거하도록 `App.jsx`에 연결했다.
- 최근 기록 기반 자동 입력, 정기지출 수정/삭제, 지출 판단 로직은 그대로 유지했다.
- `README.md`와 `docs/PROJECT.md`에 일반 지출 수정/삭제 지원 내용을 추가했다.
- `vitest`는 현재 Windows 환경의 `spawn EPERM` 제약으로 실행이 끝까지 되지 않았다.

# 다음에 해야할 일
- 브라우저에서 일반 지출 기록 목록의 수정/삭제 버튼이 보이는지 확인한다.
- 수정 클릭 시 입력 폼이 채워지고, 저장 후 기존 항목이 갱신되는지 확인한다.
- 다음 작업 전에는 커밋 메시지 승인을 먼저 받는다.

# 깃허브 푸시기록
- 이번 일반 지출 수정/삭제 보강 작업은 아직 커밋/푸시하지 않았다.

# 오늘 한 일
- 모바일 캘린더를 compact grid 형태로 줄여 날짜 셀의 세로 비중을 낮췄다.
- 빈 날짜 셀의 점선 캡슐 표현을 없애고 눈에 보이지 않게 처리했다.
- 날짜 셀 내부는 날짜를 좌상단, 금액을 하단에 두는 정렬로 바꿨다.
- 캘린더 셀 금액은 `1.1K`처럼 축약 표시되도록 바꿨고, 상태 텍스트는 따로 보여주지 않게 정리했다.
- `Calendar.jsx`와 `index.css`만 최소 수정했고, 날짜 계산 로직과 지출 데이터 구조는 건드리지 않았다.
- `README.md`의 UI 원칙에 모바일 캘린더 compact grid 안내를 추가했다.
- `vitest`는 여전히 Windows 환경의 `spawn EPERM` 제한으로 끝까지 실행되지 않았다.

# 다음에 해야할 일
- 브라우저에서 모바일 캘린더 셀이 정사각형에 가깝게 보이는지 확인한다.
- 선택된 날짜와 지출이 있는 날짜만 강조되는지 확인한다.
- 다음 작업 전에는 커밋 메시지 승인을 먼저 받는다.

# 깃허브 푸시기록
- 이번 캘린더 compact grid 개선 작업은 아직 커밋/푸시하지 않았다.

# 오늘 한 일
- 예산 설정 화면의 기능 토글 카드를 첨부 이미지처럼 보이도록 `ToggleRow` 컴포넌트로 분리했다.
- `BudgetSettings.jsx`에서 기능 토글을 체크박스 나열 대신 라벨 왼쪽, 스위치 오른쪽 구조로 바꿨다.
- `src/index.css`에 토글 스위치 전용 스타일과 모바일 대응 스타일을 추가했다.
- Vite 변환 확인으로 `BudgetSettings.jsx`, `ToggleRow.jsx`, `index.css`가 정상 응답하는 것을 확인했다.
- 토글 상태가 페이지 이동 후에도 유지되도록 즉시 저장 방식으로 바꿨다.
- `BudgetSettings.jsx`에서 토글 변경 시 부모 상태와 localStorage가 바로 갱신되도록 수정했다.
- 브랜드 로고 영역을 더 크게 보여 주도록 `brand-mark` 크기를 키웠다.
- 모바일에서는 로고가 너무 커지지 않도록 반응형 크기를 따로 두었다.

# 다음에 해야할 일
- 브라우저에서 `/#/budget-settings`를 새로고침해 토글 상태가 홈 이동 후에도 유지되는지 확인한다.
- 필요하면 스위치 크기나 카드 여백을 더 미세 조정한다.
- 다음 작업 전에는 커밋 메시지 승인을 먼저 받는다.

# 깃허브 푸시기록
- 아직 이번 토글 UI 수정은 커밋하지 않았다.

# 오늘 한 일
- 현재 워크트리의 변경 상태를 확인했다.
- 로고명과 로고 디자인 수정 요청을 반영해 현재 변경분을 원격 저장소에 푸시하기 위한 준비를 진행했다.
- 이번 작업은 코드 추가보다 푸시 및 기록 정리에 초점을 두었다.

# 다음에 해야할 일
- Toss + Banksalad 하이브리드 디자인 기준으로 화면별 구체 변경안을 정리한다.
- 캘린더 페이지 추가 범위와 데이터 표시 규칙을 확정한다.
- 이후 구현 변경이 생기면 README와 문서를 함께 동기화한다.

# 깃허브 푸시기록
- 아직 이번 작업에 대한 새 커밋/푸시는 완료되지 않았다.

# 오늘 한 일
- Toss + Banksalad 하이브리드 UI 문서에 작업 기준을 추가했다.
- 기존 기능 로직은 변경하지 않고 UI 레이아웃만 수정한다는 원칙을 명시했다.
- `index.css` 공통 토큰 우선, 카드 최소화, 핵심 숫자 먼저 노출, 모바일 기준 유지라는 작업 조건을 문서에 반영했다.

# 다음에 해야할 일
- 추가된 작업 기준에 맞춰 실제 UI 구현 순서를 정한다.
- 공통 토큰부터 적용한 뒤 화면별 레이아웃을 조정한다.
- 구현 후 README와 문서의 실제 반영 여부를 다시 맞춘다.

# 깃허브 푸시기록
- 이번 문서 보강 작업은 아직 커밋/푸시하지 않았다.

# 오늘 한 일
- Toss + Banksalad 하이브리드 UI 기준으로 화면별 변경안을 정리했다.
- 설계 문서를 `docs/superpowers/specs/2026-04-28-toss-banksalad-hybrid-ui-design.md`에 저장했다.
- 구현 계획 문서를 `docs/superpowers/plans/2026-04-28-toss-banksalad-hybrid-ui.md`에 저장했다.
- 홈/설정은 Toss형, 기록/통계는 Banksalad형으로 두되 밀도 차이가 과하게 느껴지지 않도록 공통 토큰을 공유하는 방향으로 정리했다.
- 캘린더 페이지를 기록 탐색용 읽기 화면으로 포함했다.

# 다음에 해야할 일
- 설계 문서 내용을 최종 검토한다.
- 구현 계획 문서를 기준으로 실제 React 구현 순서를 확정한다.
- 구현 시작 전에 필요하면 README와 PROJECT.md의 범위도 다시 맞춘다.

# 깃허브 푸시기록
- 이번 문서 작업은 아직 커밋/푸시하지 않았다.

# 오늘 한 일
- Toss + Banksalad 하이브리드 UI를 실제 코드에 반영했다.
- `Header`와 `BottomNav`에 `캘린더` 이동을 추가하고 하단 메뉴를 5개 항목으로 통일했다.
- 홈 화면은 큰 숫자 중심의 `MetricStrip`으로 바꾸고, 불필요한 사용자 카드 구조를 줄였다.
- 예산 설정 화면은 여백 중심 구조로 재배치하고, 기본 예산과 기능 토글을 하나의 주 영역으로 묶었다.
- 지출 기록 화면은 월간 지출/템플릿/정기지출 요약을 추가해 Banksalad형 밀도를 강화했다.
- 분석 화면은 숫자 중심 `MetricStrip`으로 재배치했다.
- 캘린더 페이지를 새로 추가해 월간 지출 그리드와 날짜별 상세를 볼 수 있게 했다.
- README에 캘린더 경로와 새 컴포넌트, 하이브리드 UI 원칙을 반영했다.

# 다음에 해야할 일
- 브라우저에서 홈, 예산, 기록, 통계, 캘린더 화면의 밀도 차이가 너무 크지 않은지 확인한다.
- 로고 크기와 헤더 폭이 새 레이아웃에서 과하지 않은지 점검한다.
- 필요하면 `MetricStrip`과 `calendar` 영역만 미세 조정한다.

# 깃허브 푸시기록
- 이번 구현 작업은 아직 커밋/푸시하지 않았다.

# 오늘 한 일
- `ExpenseRecords` 화면의 고급 옵션 토글 문구를 `달력` / `접기` 두 상태로 바꿨다.
- 버튼 문구 외의 기능 로직과 입력 흐름은 건드리지 않았다.
- README의 지출 기록 섹션에 고급 옵션 토글 표시 기준을 한 줄로 반영했다.

# 다음에 해야할 일
- 브라우저에서 `달력` / `접기` 문구가 의도한 위치에만 반영됐는지 확인한다.
- 필요하면 버튼 모양은 유지한 채 텍스트만 더 다듬는다.
- 다른 화면의 문구와 중복되지 않는지 한 번 더 점검한다.

# 깃허브 푸시기록
- 이번 수정 작업은 아직 커밋/푸시하지 않았다.

# 오늘 한 일
- `ExpenseRecords` 화면의 고급 옵션 토글 문구를 `달력`에서 `고급 옵션 펼치기 / 접기`로 원복했다.
- README에서도 고급 옵션 토글 설명을 `달력`이 아닌 `고급 옵션 펼치기 / 접기`로 다시 맞췄다.
- `Calendar` 페이지의 `달력 보기 / 접기` 문구는 그대로 유지해서 화면별 용어를 분리했다.

# 다음에 해야할 일
- 브라우저에서 `ExpenseRecords` 화면에 더 이상 `달력` 토글이 보이지 않는지 확인한다.
- `Calendar` 페이지에서만 `달력 보기 / 접기`가 보이는지 확인한다.
- 다음 작업 전에는 커밋 메시지 승인을 먼저 받는다.

# 깃허브 푸시기록
- 이번 원복 작업은 아직 커밋/푸시하지 않았다.

# 오늘 한 일
- Calendar에서 선택한 날짜를 App 상태 `expenseDraftDateKey`로 올리도록 연결했다.
- ExpenseRecords는 `selectedDateKey`를 props로 받아 현재 반영 날짜를 보여주고, 새 지출 저장 시 그 날짜를 기준으로 `record.date`를 생성하도록 바꿨다.
- 선택된 날짜가 없을 때만 오늘 날짜를 기본값으로 쓰도록 처리했다.
- 날짜 저장은 `YYYY-MM-DD` 기준으로 다루되, 저장 문자열은 `YYYY-MM-DDT00:00:00` 형태로 만들어 날짜 밀림이 없도록 맞췄다.
- 기존 지출 수정/삭제, 정기지출, 최근 기록 자동 입력 로직은 건드리지 않았다.
- 캘린더 클릭이 App으로 전달되는지와 선택 날짜 저장 흐름을 검증하는 테스트를 추가했다.

# 다음에 해야할 일
- 브라우저에서 22일 선택 후 지출 입력이 22일로 저장되는지 확인한다.
- 날짜를 선택하지 않았을 때 오늘 날짜로 저장되는지 확인한다.
- 다른 날짜 선택 후에도 해당 날짜가 저장 기준으로 반영되는지 확인한다.

# 깃허브 푸시기록
- 이번 날짜 연결 작업은 아직 커밋/푸시하지 않았다.

# 오늘 한 일
- `Calendar`에서 선택한 날짜를 `App`의 `expenseDraftDateKey` 상태로 전달하도록 연결했다.
- `ExpenseRecords`는 `selectedDateKey`를 props로 받아 선택 날짜를 표시하고, 지출 저장 시 그 날짜를 `record.date`에 반영하도록 바꿨다.
- 선택 날짜가 없으면 오늘 날짜를 기본값으로 쓰도록 처리했다.
- `YYYY-MM-DD` 기준으로 날짜를 관리하고, 저장 시 `YYYY-MM-DDT00:00:00` 형태로 만들어 날짜 밀림을 막았다.
- 기존 지출 수정/삭제와 정기지출, 최근 기록 자동 입력 로직은 변경하지 않았다.
- `Calendar`와 `ExpenseRecordsDate` 테스트 파일을 추가해 날짜 전달과 저장 날짜 반영을 검증하도록 구성했다.

# 다음에 해야할 일
- 브라우저에서 22일 선택 후 저장하면 22일에 반영되는지 확인한다.
- 날짜를 선택하지 않고 저장하면 오늘 날짜로 반영되는지 확인한다.
- 다른 날짜를 선택했을 때도 해당 날짜가 저장 기준이 되는지 확인한다.

# 깃허브 푸시기록
- `e41b5f5` `기록페이지 수정삭제 및 캘린더접기펼치기추가` - 기록 페이지 수정/삭제, 캘린더 접기/펼치기, 선택 날짜 저장 반영, 관련 문서와 테스트를 함께 반영했다.

# 오늘 한 일
- Calendar 페이지의 금액 축약 표기를 한국식 `만` 단위로 최종 변경했다.
- `12,000원 -> 1.2만`, `200,000원 -> 20만`, `20,000,000원 -> 2,000만` 기준으로 표시하도록 포맷을 고쳤다.
- 캘린더 셀, 날짜별 총액, 선택 날짜 합계, 리스트 항목 모두 같은 포맷 함수를 쓰도록 맞췄다.
- `Calendar.test.jsx`와 README의 표시 기준도 함께 갱신했다.

# 다음에 해야할 일
- 브라우저에서 캘린더 금액이 `만` 단위로 보이는지 확인한다.
- 큰 금액도 `2,000만`처럼 쉼표가 들어가는지 확인한다.
- 필요하면 소수점 반올림 기준만 추가로 조정한다.

# 깃허브 푸시기록
- 이번 수정 작업은 아직 커밋/푸시하지 않았다.

# 오늘 한 일
- Calendar 페이지의 금액 축약 표기를 `k` 단위에서 한국식 `만` 단위로 바꿨다.
- `12,000원 -> 1.2만`, `200,000원 -> 20만`, `10,000원 미만 -> 8,000원` 기준으로 표시하도록 정리했다.
- 캘린더 셀, 날짜별 총액, 날짜별 리스트 항목, 선택 날짜 합계에 같은 포맷을 적용했다.
- Calendar 테스트와 README의 표시 기준도 함께 맞췄다.

# 다음에 해야할 일
- 브라우저에서 캘린더 금액이 `만` 단위로 보이는지 확인한다.
- 날짜별 리스트와 캘린더 셀, 상세 합계의 표기가 같은지 확인한다.
- 필요하면 소수점 표기만 더 세밀하게 다듬는다.

# 깃허브 푸시기록
- 이번 수정 작업은 아직 커밋/푸시하지 않았다.

# 오늘 한 일
- 캘린더 위치를 `월 선택 / 토글 -> 캘린더 -> 월별 지출 리스트` 순서로 다시 배치했다.
- 기본 상태는 접힘 상태로 유지하고, 달력은 보조 영역으로만 펼쳐 보이도록 정리했다.
- 월별 지출 리스트는 캘린더보다 먼저 읽히도록 유지했다.
- `git diff --check`와 `Calendar.jsx` 파싱 체크로 문법 문제를 재확인했다.

# 다음에 해야할 일
- 브라우저에서 캘린더가 리스트 아래가 아니라 위에 위치하는지 확인한다.
- 달력 보기 / 접기 버튼이 계속 정상 동작하는지 확인한다.
- 선택한 날짜 섹션 강조가 유지되는지 확인한다.

# 깃허브 푸시기록
- 이번 수정 작업은 아직 커밋/푸시하지 않았다.

# 오늘 한 일
- `Calendar` 페이지를 리스트 우선 + 접히는 캘린더 구조로 다시 설계했다.
- 기본 진입 시 달력 grid가 접힌 상태가 되도록 바꾸고, 월별 지출 리스트를 먼저 보여주게 정리했다.
- 날짜별로 지출을 그룹화하고 각 날짜의 총 지출액을 표시하도록 구현 범위를 맞췄다.
- `Calendar.jsx` 문법 오류가 있었던 부분도 함께 다시 정리했다.
- README의 캘린더 설명을 실제 동작에 맞게 수정했다.

# 다음에 해야할 일
- 브라우저에서 캘린더가 기본 접힘 상태로 뜨는지 확인한다.
- 달력 보기 / 접기 토글이 정상 동작하는지 확인한다.
- 날짜별 그룹과 선택 강조가 의도대로 보이는지 확인한다.

# 깃허브 푸시기록
- 이번 수정 작업은 아직 커밋/푸시하지 않았다.

# 오늘 한 일
- `Calendar.jsx`의 파싱 에러를 수정했다.
- 캘린더 페이지를 다시 정상 JSX로 복구해 개발 서버가 트랜스폼할 수 있게 했다.
- 기능 로직과 데이터 구조는 그대로 두고 문법 오류만 바로잡았다.

# 다음에 해야할 일
- 브라우저에서 캘린더 페이지가 다시 렌더되는지 확인한다.
- 날짜 셀 compact grid가 유지되는지 점검한다.
- 필요하면 캘린더 텍스트만 추가로 다듬는다.

# 깃허브 푸시기록
- 이번 수정 작업은 아직 커밋/푸시하지 않았다.
# 오늘 한 일
- 일반 지출과 정기지출 카테고리 배열을 `src/lib/categories.js`로 분리했다.
- `ExpenseRecords.jsx`의 select를 배열 기반 렌더링으로 바꾸고, 기존 category 값이 목록 밖일 때는 `기존: {category}` 옵션이 보이도록 처리했다.
- 최근 기록 자동 입력, 저장/수정/삭제, 선택 날짜 저장 흐름은 유지했다.
- `src/pages/ExpenseRecords.jsx`와 `src/pages/ExpenseRecords.test.jsx`에 대해 `esbuild` 파싱 체크를 통과했다.
- `git diff --check`는 치명적인 패치 오류 없이 통과했다.

# 다음에 해야할 일
- 브라우저에서 일반 지출 카테고리 15개, 정기지출 카테고리 5개가 보이는지 확인한다.
- 기존 category fallback 표시가 실제 수정/최근입력 모드에서도 깨지지 않는지 확인한다.
- 필요하면 관련 테스트를 한 번 더 좁게 점검한다.

# 깃허브 푸시기록
- 아직 없음.
# 오늘 한 일
- `MVP 입력` 문구를 `지출 기록`으로 바꾸고, `정기지출 등록` 문구를 `정기지출 관리`로 변경했다.
- 지출 기록 화면 안내 문구를 사용자 중심 표현으로 다듬었다.
- README의 지출 기록 섹션과 프로젝트 구조를 실제 화면 문구에 맞게 동기화했다.
- `src/pages/ExpenseRecords.jsx`에 대해 `esbuild` 파싱 체크를 진행할 예정이다.

# 다음에 해야할 일
- 브라우저에서 지출 기록 / 정기지출 관리 문구가 의도대로 바뀌었는지 확인한다.
- 기존 기능 구조와 입력 흐름이 그대로 유지되는지 살펴본다.

# 깃허브 푸시기록
- 아직 없음.
