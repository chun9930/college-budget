## 2026-05-03 Statistics 월 선택 QA
1. 오늘 한 일
- Statistics 화면에 월 선택 기능을 추가했다.
- `selectedMonth`를 기준으로 선택 월 통계, 전월 비교, 소비 패턴 피드백, 대학생 절약 팁이 다시 계산되도록 정리했다.
- 월 선택 UI와 선택 월 기준 문구를 README에 반영했다.
- esbuild 문법 검증과 React server 렌더 기준으로 월 선택 노출을 확인했다.
- Vitest는 여전히 `spawn EPERM`으로 실행되지 않았다.

2. 다음에 해야할 일
- 필요 시 Statistics 화면의 월 선택 UX를 브라우저에서 추가 확인한다.
- 다른 페이지에 영향이 없는지 최종 점검한다.

3. 깃허브 푸시기록
- 없음

## 2026-05-03 Statistics 레이아웃 개선
1. 오늘 한 일
- Statistics 화면의 `카테고리별 지출 비중` 카드가 상단부터 시작하도록 레이아웃을 조정했다.
- Statistics 전용 grid 정렬 클래스를 추가해 왼쪽 카드가 오른쪽 컬럼 높이에 끌려 늘어나는 문제를 줄였다.
- README의 분석 리포트 섹션에 상단 정렬/균형 배치 설명을 최소 문구로 보강했다.
- esbuild 문법 검증을 통과했다.
- Vitest는 이번에도 `spawn EPERM`으로 실행되지 않았다.

2. 다음에 해야할 일
- 브라우저에서 Statistics 화면의 카테고리 비중 카드 상단 여백이 줄었는지 확인한다.
- 필요하면 15번 QA에서 시각 배치만 최종 점검한다.

3. 깃허브 푸시기록
- 없음

## 2026-05-03 BudgetSettings 목표 명칭 정리
1. 오늘 한 일
- BudgetSettings의 탭 이름을 `예산 계산 설정` / `목표 계획 목록`으로 바꾸고, 목표 관련 카드 제목을 `월 저축 계획`으로 변경했다.
- 기본 예산 설정 탭과 목표 계획 목록 탭의 역할 차이를 설명 문구로 분리했다.
- README의 예산 설정 설명도 새 명칭과 동일한 의미로 맞췄다.
- esbuild 문법 검증은 통과했지만, Vitest는 Windows/Vite 환경의 `spawn EPERM`으로 실패했다.

2. 다음에 해야할 일
- 브라우저에서 `예산 계산 설정`과 `목표 계획 목록` 탭 이름이 자연스럽게 보이는지 확인한다.
- `월 저축 계획`과 `목표 계획 목록`의 역할 차이가 화면에서 분명하게 읽히는지 확인한다.

3. 깃허브 푸시기록
- 없음

## 2026-05-03 BudgetSettings 목표 역할 명칭 정리
1. 오늘 한 일
- BudgetSettings의 목표 관련 명칭을 `월 저축 계획`과 `목표 계획 목록`으로 더 분명하게 구분했다.
- `예산 계산 설정` 탭에는 하루 예산 계산에 직접 반영되는 항목만 남기고, 목표 계획 목록은 계획용 카드로 설명을 붙였다.
- README의 예산 설정 설명도 새 명칭 기준으로 다시 맞췄다.
- esbuild는 통과했고, Vitest는 계속 `spawn EPERM`으로 실행되지 않았다.

2. 다음에 해야할 일
- 브라우저에서 `예산 계산 설정`과 `목표 계획 목록` 탭 문구가 자연스럽게 읽히는지 확인한다.
- 필요하면 15번 전체 QA에서 예산 설정 화면만 최종 확인한다.

3. 깃허브 푸시기록
- 없음

## 2026-05-03 BudgetSettings 문구 복구
1. 오늘 한 일
- BudgetSettings 화면의 깨진 한글 문구를 정상 한국어로 복구했다.
- 예산 설정, 수입 입력 방식, 수동 고정지출, 이월 금액, 목표 기반 재무 계획 관련 안내 문구를 정상 표기로 바꿨다.
- README의 예산 설정 설명에 한국어 라벨 안내를 보강했다.
- esbuild로 BudgetSettings 문법 검증을 진행했다.
- BudgetSettings.test.jsx와 App.test.jsx를 다시 실행해 통과했다.
- 브라우저 직접 확인은 아직 미실행이다.

2. 다음에 해야할 일
- 브라우저에서 BudgetSettings 화면의 제목/설명/입력 라벨이 정상 한국어로 보이는지 확인한다.
- 필요하면 15번 QA에서 예산 설정 화면만 최종 점검한다.

3. 깃허브 푸시기록
- 없음

## 2026-05-03 BudgetSettings 목표 카드 레이아웃 개선
1. 오늘 한 일
- BudgetSettings의 목표 기반 재무 계획 카드 정보를 요약/세부 영역으로 나눠 가독성을 개선했다.
- 목표명, 카테고리, 목표/현재 저축액, 달성률, 남은 금액, 마감일, 남은 기간, 하루/월별 필요 저축액, 수정/삭제 버튼이 한 카드 안에서 더 자연스럽게 보이도록 정리했다.
- 모바일에서는 목표 상세 영역이 1열로 내려가도록 CSS를 보강했다.
- README의 목표 기반 재무 계획 설명을 카드 목록 기준으로 맞췄다.

2. 다음에 해야할 일
- 브라우저에서 목표 카드가 1개일 때와 여러 개일 때 모두 자연스럽게 보이는지 확인한다.
- 필요하면 15번 QA에서 목표 카드만 최종 점검한다.

3. 깃허브 푸시기록
- 없음

## 2026-05-03 ExpenseRecords 문구 정리
1. 오늘 한 일
- ExpenseRecords 화면의 일반 지출 / 정기지출 설명 문구를 더 자연스러운 한국어로 다듬었다.
- 일반 지출은 실제 생활 소비를 기록하는 곳, 정기지출은 월세/통신비/구독료 같은 반복 고정비라는 점을 더 명확하게 바꿨다.
- 자동 정기지출 배지와 예산 계산 중복 제외 안내는 그대로 유지했다.
- README의 지출 기록 설명도 동일한 문구 기준으로 맞췄다.
- esbuild로 ExpenseRecords 문법 검증을 통과했다.

2. 다음에 해야할 일
- 브라우저에서 지출 기록 화면의 일반 지출 / 정기지출 설명이 자연스럽게 보이는지 확인한다.
- 15번 전체 QA에서 문구/레이아웃을 최종 점검한다.

3. 깃허브 푸시기록
- 없음

## 2026-05-03 전체 QA 체크리스트 재검증
1. 오늘 한 일
- Home, BudgetSettings, ExpenseRecords, Statistics, Calendar, MyPage의 전체 QA 체크리스트를 재검증했다.
- 브라우저 직접 확인 도구는 현재 세션에서 사용할 수 없어, 코드 기준 확인과 자동 검증으로 대체했다.
- `ExpenseRecords`의 일반 지출 / 정기지출 문구는 최종 QA 기준으로 다시 확인했다.
- esbuild 검증은 통과했고, Vitest는 Windows/Vite 환경의 `spawn EPERM`으로 계속 실패했다.
- 브라우저 직접 QA는 미실행으로 남겼다.

2. 다음에 해야할 일
- 브라우저 도구가 가능한 환경에서 Home, BudgetSettings, ExpenseRecords, Statistics, Calendar, MyPage를 최종 시각 확인한다.
- 필요 시 문구나 레이아웃의 미세 조정만 수행한다.

3. 깃허브 푸시기록
- 없음

## 2026-05-03 BudgetSettings 탭 분리 및 문구 정리
1. 오늘 한 일
- BudgetSettings 화면에 `기본 예산 설정` / `목표 관리` 탭을 추가해 목표 목록과 목표 폼을 분리했다.
- 기본 예산 설정 탭에는 월수입, 수동 고정지출, 비상금, 이월, 기능 토글만 남기고 목표 목록은 별도 탭으로 옮겼다.
- 수동 고정지출 / 정기지출 / 예산에 반영되는 고정지출 문구를 사용자 기준으로 다시 정리했다.
- README의 예산 설정 설명도 탭 구조와 새 문구에 맞게 갱신했다.
- esbuild와 BudgetSettings/App 테스트를 다시 실행해 통과했다.

2. 다음에 해야할 일
- 브라우저에서 BudgetSettings의 기본 예산 설정 / 목표 관리 탭 전환이 자연스러운지 확인한다.
- 목표 카드가 넓게 보이는지, 고정지출 문구가 변수명 없이 읽히는지 확인한다.

3. 깃허브 푸시기록
- 없음
